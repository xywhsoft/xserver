# XServer 更新记录

以版本号锚定，随发布滚动维护。扩展库的上游基线见各 `lib/<库>/UPSTREAM.txt`；
构建与验证细节见 [可选扩展库](/D:/GIT/xserver/docs/可选扩展库.md) 的验证记录一节。

## v1.0.0（2026-09-15）

首个定版。基线 commit `5f61aee`。

### 可选扩展库：3 → 10 个（共 690 个导入符号）

| 扩展 | 符号 | 变体名 | 说明 |
|---|---|---|---|
| SQLite | 274 | `sqlite` | 既有 |
| XTP2 | 13 | `xtp` | 既有 |
| xllm | 53 | `xllm` | LLM 调用核心库（同步至 v3：私建 CA、store wire、枚举增量） |
| xllm-session | 45 | `xllm-session` | **新增** 会话治理层 v3：上下文预算、压缩/梯子、治理帧、journal |
| xmail | 89 | `xmail` | **新增** 邮件 MIME 基础层（编解码/地址/消息树/构造/传输/SASL） |
| xsmtp | 44 | `xsmtp` | **新增** SMTP 客户端（requires: xmail） |
| xpop3 | 48 | `xpop3` | **新增** POP3 客户端（requires: xmail） |
| ximap | 102 | `ximap` | **新增** IMAP 客户端（requires: xmail） |
| md4c | 2 | `md4c` | **新增** CommonMark Markdown 解析 + HTML 渲染 |
| xacme | 20 | `xacme` | **新增** ACME/RFC 8555 证书签发客户端（dns-01，内建阿里云 provider） |

### 构建系统

- **`all` 全选参数**：`build.bat all` / `bash build.sh all` 一键编译全部可选库。
- **`requires` 依赖自动展开**：注册表声明依赖（xsmtp/xpop3/ximap → xmail），选库阶段
  DFS 闭包展开 + 环检测；组合构建时依赖只编译注册一次，变体目录名如实展开。
- **`feature_macro` 多宏列表** 与 **`host_includes`** 两个注册表字段：多特性宏库
  （xacme）与带 include 路径的扩展无需改构建驱动。
- **xrt 模块头垫片层 SDK 常驻**（`lib/xrtshim/`）：把 `<xrt/*.h>` 模块头映射到
  `lib/xrt_decl.h` 无门控全量声明；`promote.h` 按真实 xrt.h 语义把 `XRT_MODULE_*`
  持久提升为 `XRT_FEATURE_*`。VFS 固定打包 `/xs/xrt/*.h` 与 `/xs/xrt.h`。
- xrt 升级为主线纯快照 `2639487c`（xrt 符号 2,963 → 3,030；零分叉策略见
  `lib/xrt_version.txt`）。

### 运行时

- **启动横幅与 `--version`**：`[xs] XServer 1.0.0 (commit, 日期, 平台)` + 按注册表
  顺序列出本变体扩展清单（default 显示 none）。commit/日期/平台由构建期注入，
  版本号见 `src/core/xs_version.h`。
- **契约函数 `xsExtensionEnabled(name)`**（xs 层 API 第 21 个）：运行期按注册表名
  查询扩展是否编入；大小写不敏感、requires 可见、未知名/空串/NULL 恒 false。

### 其它

- VFS 资源（all 变体）268 → 350 项；Windows 全量二进制约 5.98 MB。
- LICENSE：开源许可说明——自有代码 MIT，捆绑的第三方组件（TCC 为 LGPL 等）保留原许可。
- demo-chat 接入 xllm-session 会话治理层。

## 此前（v1.0.0 之前，未定版期间的主要节点）

- 2026-09-12：累积更新（pattern 路由、扩展体系、基准、双 libc 构建等，见 git 历史）。
- 2026-09-02：修复 UDP 重启后无法接收数据的问题，解决内存 GC 问题。
