/*
 * demo_extra.h — dev_inc 搜索路径示例头
 *
 * 本目录（demo-single/includes）已通过 xs.json 的 dev_inc 注册为
 * TCC 头文件搜索路径，脚本内可直接 #include <demo_extra.h> 引用，
 * 无需相对路径。尖括号写法可验证搜索路径确实生效。
 */

#ifndef DEMO_EXTRA_H
#define DEMO_EXTRA_H

#define DEMO_EXTRA_TEXT		"demo-single ok (from includes/demo_extra.h)"

#endif
